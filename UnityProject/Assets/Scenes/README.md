# Scenes

- `TitleScene` — Presents New Game and Continue buttons via the `TitleScreenController` component.
- `MainScene` — Hosts the player prefab with `PlayerController`, `Interactor`, and `Flashlight`.
